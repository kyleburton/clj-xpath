require 'java'
import com.github.kyleburton.clj_xpath

describe clj_xpath do
  it "should parse a valid xml documents" do
  end

  it "should raise an exception for invalid xml documents" do
  end

  it "should be able to extract the top level tag" do
  end

  describe "singleton operators" do

    it "should raise an erorr when no elements are matched" do
    end

    it "should return the single node when only one is matched" do
    end

    it "should raise an error when more than 1 element is matched" do
    end
  end

end
